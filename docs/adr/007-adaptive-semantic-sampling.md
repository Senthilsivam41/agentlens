# ADR-007: Adaptive Semantic Sampling

**Status:** Accepted

Structural scoring covers every eligible execution. Semantic scoring covers all errors, high-volatility traces, unknown paths, new versions, and a deterministic sample of normal traffic. Every score stores sampling policy, reason, and coverage.

